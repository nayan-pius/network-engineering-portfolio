# ⚙️ Device Configurations (Backups)
**Standard:** Cisco IOS | **Format:** Plain Text (`.txt` / `.cfg`)

This directory stores the `running-config` exports from routers and switches. In a real-world production environment, these files are critical for **Disaster Recovery** and **Change Management**.

## 📝 Naming Convention
`[Day]_[DeviceType]_[Hostname].txt`
* *Example:* `Day03_Router_Gateway-R1.txt`

## 💡 Usage
These configurations can be directly pasted into a device CLI to replicate the network state.